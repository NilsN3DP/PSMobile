<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Nur der Freigabeordner, nicht der ganze Cache: was geteilt wird,
         soll bewusst dorthin kopiert werden. -->
    <cache-path name="share" path="share/" />
    <cache-path name="gcode-out" path="gcode-out/" />
    <external-files-path name="projects" path="Projects/" />
    <files-path name="projects-internal" path="Projects/" />
</paths>
