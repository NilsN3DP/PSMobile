<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Netzwerk fuer PrusaLink/PrusaConnect. Bewusst nativ ueber OkHttp
         statt libcurl - siehe docs/entscheidungen.md, E-09. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- OpenGL ES 3.0 ist die Basis fuer den Viewport (M4). -->
    <uses-feature android:glEsVersion="0x00030000" android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:largeHeap="true"
        android:theme="@style/Theme.PSMobile">

        <!-- singleTask: ohne das erzeugt jeder eingehende VIEW-Intent eine
             zweite Activity-Instanz, deren onServiceConnected das Modell ein
             zweites Mal importiert. Befund A5 in docs/09-fehlerliste.md. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|density"
            android:theme="@style/Theme.PSMobile">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Modelle aus anderen Apps entgegennehmen -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" android:mimeType="*/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
        </activity>

        <!--
            Slicing laeuft in einem Foreground-Service, damit der Job
            weiterlaeuft, wenn der Bildschirm ausgeht - ein Slice kann auf
            dem Handy mehrere Minuten dauern.

            TODO (M7): android:process=":slicer" ergaenzen, damit der
            Low-Memory-Killer bei grossen Modellen nur den Job trifft und
            nicht die App. Das setzt voraus, dass die Session per Messenger
            statt per lokalem Binder angesprochen wird.
        -->
        <service
            android:name=".slicing.SlicerService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Zum Teilen des fertigen G-Code. Seit Nougat duerfen
             file://-URIs nicht mehr an andere Apps gehen. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
