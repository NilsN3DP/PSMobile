diff --git a/deps/+OCCT/OCCT.cmake b/deps/+OCCT/OCCT.cmake
index 81cdf46..a34b2e9 100644
--- a/deps/+OCCT/OCCT.cmake
+++ b/deps/+OCCT/OCCT.cmake
@@ -7,6 +7,11 @@ add_cmake_project(OCCT
 
     PATCH_COMMAND ${CMAKE_COMMAND} -E copy ${CMAKE_CURRENT_LIST_DIR}/occt_toolkit.cmake ./adm/cmake/
     CMAKE_ARGS
+        # OCCT 7.6.1 verlangt cmake_minimum_required(VERSION 3.0).
+        # CMake 4 lehnt das ab; der Container auf dem Build-Host hat
+        # noch eine aeltere Fassung, auf dem Mac scheitert sonst schon
+        # die Konfiguration.
+        -DCMAKE_POLICY_VERSION_MINIMUM=3.5
         -DINSTALL_DIR_LAYOUT=Unix # LMBBS
         -DBUILD_LIBRARY_TYPE=Static
         -DUSE_TK=OFF
diff --git a/deps/CMakeLists.txt b/deps/CMakeLists.txt
index 04c8c8e..be7e42b 100644
--- a/deps/CMakeLists.txt
+++ b/deps/CMakeLists.txt
@@ -87,7 +87,10 @@ if (MSVC)
     dep_message(FATAL_ERROR "Unable to detect architecture!")
   endif ()
 else ()
-    set(DEP_CMAKE_OPTS "-DCMAKE_POSITION_INDEPENDENT_CODE=ON")
+    # PSMobile: mehrere Pakete (OCCT, Blosc, ...) verlangen
+    # cmake_minimum_required(VERSION 3.0). CMake 4 lehnt das ab.
+    set(DEP_CMAKE_OPTS "-DCMAKE_POSITION_INDEPENDENT_CODE=ON"
+                       "-DCMAKE_POLICY_VERSION_MINIMUM=3.5")
 endif ()
 
 if (APPLE)
@@ -113,6 +116,9 @@ if (APPLE)
     
     set(DEP_CMAKE_OPTS
         "-DCMAKE_POSITION_INDEPENDENT_CODE=ON"
+        # PSMobile: siehe oben - hier wird die Liste vollstaendig
+        # neu gesetzt, die Regel muss also auch hier stehen.
+        "-DCMAKE_POLICY_VERSION_MINIMUM=3.5"
         "-DCMAKE_OSX_SYSROOT=${CMAKE_OSX_SYSROOT}"
         "-DCMAKE_OSX_DEPLOYMENT_TARGET=${CMAKE_OSX_DEPLOYMENT_TARGET}"
         "-DCMAKE_OSX_ARCHITECTURES=${CMAKE_OSX_ARCHITECTURES}"
