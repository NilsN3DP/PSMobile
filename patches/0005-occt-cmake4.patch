diff --git a/deps/+OCCT/OCCT.cmake b/deps/+OCCT/OCCT.cmake
--- a/deps/+OCCT/OCCT.cmake
+++ b/deps/+OCCT/OCCT.cmake
@@ -5,6 +5,11 @@ add_cmake_project(OCCT
     PATCH_COMMAND ${CMAKE_COMMAND} -E copy ${CMAKE_CURRENT_LIST_DIR}/occt_toolkit.cmake ./adm/cmake/
     CMAKE_ARGS
+        # OCCT 7.6.1 verlangt cmake_minimum_required(VERSION 3.0).
+        # CMake 4 lehnt das ab. Der Container auf dem Build-Host hat noch
+        # eine aeltere Fassung, auf dem Mac scheiterte sonst schon die
+        # Konfiguration - fuer denselben Quellcode.
+        -DCMAKE_POLICY_VERSION_MINIMUM=3.5
         -DINSTALL_DIR_LAYOUT=Unix
         -DBUILD_LIBRARY_TYPE=Static
         -DUSE_TK=OFF -DUSE_TBB=OFF -DUSE_FREETYPE=OFF -DUSE_FFMPEG=OFF -DUSE_VTK=OFF
